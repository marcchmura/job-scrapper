name: Job Scraper

on:
  schedule:
    # 08:00 Frankfurt summer time ~= 06:00 UTC
    - cron: "0 8 * * *"

    # 18:00 Frankfurt summer time ~= 16:00 UTC
    - cron: "0 16 * * *"

  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browsers
        run: |
          playwright install --with-deps chromium

      - name: Run scraper
        run: |
          python main.py
